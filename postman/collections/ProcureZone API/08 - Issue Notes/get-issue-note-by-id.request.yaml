$kind: http-request
name: Get Issue Note By ID
method: GET
url: '{{baseUrl}}/api/v1/issue-notes/{{issueNoteId}}'
order: 3000
auth:
  type: bearer
  credentials:
    - key: token
      value: '{{authToken}}'
